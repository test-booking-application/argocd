apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: user-service
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  
  source:
    repoURL: https://github.com/test-booking-application/user-service.git
    targetRevision: main
    path: charts/user-service
    helm:
      releaseName: user-service
      values: |
        image:
          repository: 044302809167.dkr.ecr.us-east-1.amazonaws.com/ticket-booking/user-service
          tag: latest
          pullPolicy: IfNotPresent
        
        replicaCount: 2
        
        service:
          type: ClusterIP
          port: 3001
        
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 250m
            memory: 256Mi
  
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
